// Placeholder prisma.config.js created during migration attempts.
// Leaving an empty config so prisma CLI doesn't fail parsing; the real
// datasource URL will be passed at runtime via the PrismaClient constructor.

module.exports = {};
