/**
 * iCash Frontend Configuration
 * Production uses the separately deployed liveness service.
 */
window.ICASH_CONFIG = {
  API_BASE_URL: '',
  // Override this for local/self-hosted deployments.
  LIVENESS_URL: 'https://icash-liveness.onrender.com',
};
